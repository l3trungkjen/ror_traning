<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <%= link_to 'Home', root_path, class: 'navbar-brand' %>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <% if @user_profile.blank? %>
                    <li>
                        <%= link_to 'Signin', sessions_path %>
                    </li>
                <% else %>
                    <li class="dropdown">
                        <%= link_to '#', class: 'dropdown-toggle', :data => { toggle: 'dropdown' } do %>
                            Entries
                            <%= content_tag(:span, '', :class => 'caret') %>
                        <% end %>
                        <ul class="dropdown-menu" role="menu">
                            <li><%= link_to 'Add new', new_entry_path %></li>
                            <li><%= link_to 'Show all', entries_path %></li>
                        </ul>
                    </li>
                    <li class="dropdown">
                        <%= link_to '#', class: 'dropdown-toggle', :data => { toggle: 'dropdown' } do %>
                            Users
                            <%= content_tag(:span, '', :class => 'caret') %>
                        <% end %>
                        <ul class="dropdown-menu" role="menu">
                            <li><%= link_to 'Show all', users_path %></li>
                        </ul>
                    </li>
                    <li class="dropdown">
                        <%= link_to '#', class: 'dropdown-toggle', :data => { toggle: 'dropdown' } do %>
                            <%= @user_profile.email %>
                            <%= content_tag(:span, '', class: 'caret') %>
                        <% end %>
                        <ul class="dropdown-menu" role="menu">
                            <li><%= link_to 'Profile', session_path(@user_profile) %></li>
                            <li><%= link_to 'Settings', edit_session_path(@user_profile) %></li>
                            <li class="divider"></li>
                            <li class="dropdown-header"><%= link_to 'Sign out', session_path(@user_profile), method: :delete %></li>
                        </ul>
                    </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>