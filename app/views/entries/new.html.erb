<%= form_for :entry, :html => { class: 'form-signup' }, method: 'post', url: entries_path do |f| %>
    <h2>Create Entries</h2>
    <% if @entry.errors.any? %>
        <% @entry.errors.full_messages.each do |message| %>
            <p class="bg-danger"><%= message %></p>
        <% end %>
    <% end %>
    <div class="form-group">
        <%= f.label :Title %>
        <%= f.hidden_field :user_id, value: @user_profile.id %>
        <%= f.text_field :title, class: 'form-control', placeholder: 'Input title...' %>
    </div>
    <div class="form-group">
        <%= f.label :Body %>
        <%= f.text_area :body, class: 'form-control', placeholder: 'Input body...' %>
    </div>
    <%= f.submit 'Create', class: 'btn btn-success' %>
<% end %>